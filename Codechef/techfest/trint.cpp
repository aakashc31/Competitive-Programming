#include <bits/stdc++.h>
using namespace std;
#define f(i,n) for(int i=0; i<n; i++)
#define g(n) scanf("%d",&n)
#define mp make_pair


int ar[1000]
int main()
{
	int t,n,m;
	while(t--)
	{
		g(n); g(m);
		f(i,n) g(ar[i]);
		sort(ar,ar+n);
		f(i,n)
		{
			for(int j=i+1; j<n; j++)
			{
				
			}
		}
	}
}
#include <bits/stdc++.h>

using namespace std;

typedef long long int lli;

#define get(n) scanf("%lld",&n)
#define f(i,n) for(lli i=0;i<n;i++)

int main()
{
	lli a,b;
	get(a),get(b);
	printf("%lld\n",a+b);
	return 0;
}
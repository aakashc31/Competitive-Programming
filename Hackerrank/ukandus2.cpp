#include <iostream>
#include <cstring>
#include <cstdio>

#define getNum(n) scanf("%d",&n)
#define f(i,n) for(int i=0; i<n;i++)
using namespace std;

int main()
{
	int n;
	getNum(n);
	char lines[10][400];
	f(i,n)
		gets(lines[i]);
	int t;
	getNum(t);
	char word[]

}